import React from "react";
import styled from "styled-components";

const Container = styled.div``;

const Movies = () => {
  return <Container>Popular, Now Playing, latest, upcomming</Container>;
};

export default Movies;
